# Checklist: send and stream

Covers [sending-a-message.md](../features/sending-a-message.md), [streaming-and-reading.md](../features/streaming-and-reading.md), [turn-reservation.md](../foundations/turn-reservation.md).

| ID    | P   | Device         | Setup → steps                                                           | Expected                                                                                                          | Automated                                                                                             | Result |
| ----- | --- | -------------- | ----------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------ |
| SS-1  | P1  | desktop        | Ongoing conversation, at bottom → send a short prompt                   | One continuous glide; sent message settles ~50px below the view top; blank space below to the composer            | `chatScroll` "send anchors the pair at the anchor offset"                                             |        |
| SS-2  | P1  | desktop        | After SS-1, while the reply streams within the blank space              | Nothing moves: message, reply top, scrollbar thumb all still; tokens fill downward                                | `chatScroll` "fill phase: constant height, zero movement, zero layout shift"                          |        |
| SS-3  | P1  | desktop        | Prompt long enough to outgrow the view ("write 1000 words"), fast model | Stillness throughout: the reply grows below the fold and the beginning stays put; the ↓ button appears past 200px | `chatScroll` "read mode: a reply outgrowing its reservation never moves the view"                     |        |
| SS-4  | P1  | desktop        | In read mode, press ↓; then, while following, wheel up a notch          | ↓ glides to the live bottom and follows; the wheel stops it instantly and the view stays; stream continues below  | `chatScroll` "the jump button engages following during read mode"; `stickToBottom` "wheel up unpins…" |        |
| SS-5  | P1  | desktop        | End of a long reply, view following                                     | Stream end moves nothing; late highlighting/images at the bottom are followed without a jump                      | `chatScroll` "settling keeps the view still"                                                          |        |
| SS-6  | P1  | touch          | At bottom → send from the keyboard                                      | Snap (no glide) to the exchange; keyboard closes; anchored message correctly placed in the restored view          | hand-only (visualViewport)                                                                            |        |
| SS-7  | P2  | desktop        | Scroll far up mid-stream → send                                         | View comes down in one glide to the new exchange                                                                  | `chatScroll` "send re-attaches a detached reader"                                                     |        |
| SS-8  | P2  | desktop        | Send, and wheel up during the glide                                     | Glide cancels immediately; no yank when the pair mounts; exchange proceeds below                                  | `chatScroll` "scrolling up during a send revokes its pin"                                             |        |
| SS-9  | P2  | desktop        | Send with a large attachment (slow encode)                              | Glide reaches bottom, placeholder appears; exchange anchors on mount via the follow                               | `chatScroll` "send still anchors when the pair mounts late"                                           |        |
| SS-10 | P2  | desktop        | Type a multi-line draft while a reply is filling                        | Composer grows; reply text never occluded; page height constant (no motion)                                       | `chatScroll` "composer growth during fill is height-neutral"                                          |        |
| SS-11 | P2  | reduced-motion | SS-1 with reduced motion on                                             | Same positions, instant moves                                                                                     | `stickToBottom` "reduced motion makes every move instant"                                             |        |
| SS-12 | P3  | desktop        | First message of a brand-new conversation                               | Anchors like any turn (message near top, space below); navigation to the new route shows no visible reset         | `chatScroll` "first exchange anchors like any other"                                                  |        |
| SS-13 | P3  | desktop        | Wheel up inside a tall code block mid-stream (block scrolled down)      | Code block scrolls; conversation stays following                                                                  | `stickToBottom` "wheel over inner scrollable"                                                         |        |
| SS-14 | P1  | desktop        | DevTools performance panel recording SS-2/SS-3                          | Zero layout-shift entries without recent input                                                                    | harness CLS probe                                                                                     |        |
